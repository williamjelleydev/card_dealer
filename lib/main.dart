import 'package:flutter/material.dart';
import 'package:card_dealer/Pages/HomePage.dart';

void main() {
  runApp(CardDealer());
}

class CardDealer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new MaterialApp(
      title: 'Card Dealer',
      home: HomePage(),
    );
  }
}


// Right...
// I should _probably_ do some rough sketchups of what I want this to look like
// Before I jump right in..?





